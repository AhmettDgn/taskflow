name: Deploy

on:
  push:
    branches:
      - main

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run unit tests
        run: pnpm test:unit

      - name: Run integration tests
        run: pnpm test:integration

      - name: Build workspace
        run: pnpm build

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - name: Sunucuya Baglan ve PM2 ile Guncelle
        # Bu eklenti bizim yerimize tum SSH ve Known_hosts islerini halleder!
        uses: appleboy/ssh-action@v1.0.3
        with:
          # Github Secrets'taki mevcut degiskenlerin:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 1. Proje yoluna git (Takım arkadaşın Yusuf'un dizini)
            cd /home/yusuf/projects/taskflow
            
            # 2. GitHub'dan yeni kodlari cek
            git pull origin main
            
            # 3. pnpm ile paketleri kur ve derle
            corepack enable
            pnpm install --frozen-lockfile
            pnpm build
            
            # 4. PM2 ile projeyi kesintisiz yeniden baslat
            pm2 reload taskflow